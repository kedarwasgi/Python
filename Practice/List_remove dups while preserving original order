#remove dups while preserving original order

def remove_dups(lst):
    new_lst = []
    for i in lst:
        if i not in new_lst:
             new_lst.append(i)
        else:
            continue
    return new_lst

lst = [1,2,2,4,5,6,6,3]
print(remove_dups(lst))
